# Task 12.5 – CriseDetailPanel, EventoDetailPanel, Timeline page e TopNav

## Objetivo
Implementar os painéis deslizantes de detalhe (Framer Motion), a página Timeline completa que integra todos os componentes e a atualização do TopNav com o link "Linha do Tempo".

## Tipo
frontend-pagina

## Arquivos a criar/modificar
- `src/components/timeline/CriseDetailPanel.tsx` → criar
- `src/components/timeline/EventoDetailPanel.tsx` → criar
- `src/pages/Timeline.tsx` → criar
- `src/components/layout/TopNav.tsx` → modificar (link "Linha do Tempo")
- `src/router.tsx` (ou equivalente) → modificar (rota `/dashboard/timeline`)

## Contexto necessário (arquivos para ler antes)
- `src/hooks/useTimeline.ts` → (12.3)
- `src/hooks/useCriseDetalhe.ts` → (12.3)
- `src/types/timeline.ts` → (12.3)
- `src/components/timeline/TimelineBar.tsx` → interface de callbacks (12.4)
- `src/components/layout/TopNav.tsx` → estrutura da navegação

## Skills necessárias
- frontend-design-system
- animacoes-motion

## Especificação

### O que fazer:

**`CriseDetailPanel.tsx`:**
Props: `slug: string | null`, `onClose: () => void`
- Usar `useCriseDetalhe(slug)` internamente
- Painel deslizante da direita (Framer Motion):
  ```tsx
  <AnimatePresence>
    {slug && (
      <motion.div
        initial={{ x: '100%' }}
        animate={{ x: 0 }}
        exit={{ x: '100%' }}
        transition={{ type: 'spring', damping: 25, stiffness: 200 }}
        className="fixed right-0 top-0 h-full w-[480px] bg-[#1C1F26] overflow-y-auto z-50"
      >
  ```
- Overlay escuro clicável para fechar
- Skeleton loading enquanto `isLoading`
- **3 seções:**
  1. **Contexto Geopolítico:** título + `contexto_geopolitico` (texto completo)
  2. **Impacto Global:** `impacto_global` + grid de `metricas_globais` (label + valor em destaque)
  3. **Impacto no Brasil:** `impacto_brasil` + grid de `metricas_brasil`
- Header: `titulo` + `ano` + badges de `categorias`
- Se `content_slug`: botão "Ver na Biblioteca →" com link para `/biblioteca/{content_slug}`
- Botão fechar (X) no canto superior direito

**`EventoDetailPanel.tsx`:**
Props: `eventoId: number | null`, `onClose: () => void`
- Painel mais simples (mesma animação, largura 380px)
- Buscar evento localmente dos dados já carregados (sem query adicional)
- Exibir: `titulo`, `nivel_tensao` (badge), `created_at` formatado, `descricao`
- Link "Ver no Feed →" para `/dashboard/feed/{id}`

**`Timeline.tsx` (página):**
Estado local:
```typescript
const [slugSelecionado, setSlugSelecionado] = useState<string | null>(null);
const [eventoIdSelecionado, setEventoIdSelecionado] = useState<number | null>(null);
const [filtros, setFiltros] = useState<FiltrosTimeline>({});
```
- `useTimeline(filtros)` para buscar dados
- **Barra de filtros:** select de categoria + range de anos (dois inputs number ou um slider)
- `TimelineBar` recebendo dados e callbacks
- `CriseDetailPanel` com `slug={slugSelecionado}` e `onClose={() => setSlugSelecionado(null)}`
- `EventoDetailPanel` com `eventoId={eventoIdSelecionado}` e `onClose={() => setEventoIdSelecionado(null)}`
- Título da página: "Linha do Tempo de Crises"
- Legenda das faixas: quadrado dourado "Crises Históricas" + quadrado branco "Eventos Ativos"

**`TopNav.tsx`:**
- Adicionar link "Linha do Tempo" com ícone de relógio/cronômetro
- Rota: `/dashboard/timeline`

**Rota:**
```tsx
{ path: '/dashboard/timeline', element: <Timeline /> }
```

### Regras de negócio aplicáveis:
- Apenas um painel aberto por vez (fechar um ao abrir o outro)
- Painel fecha ao clicar no overlay ou no botão X
- `AnimatePresence` do Framer Motion para animação de entrada/saída

### Critérios de aceitação:
- [ ] Clicar em crise abre `CriseDetailPanel` com animação
- [ ] 3 seções do painel preenchidas corretamente
- [ ] Métricas exibidas em destaque visual
- [ ] Fechar painel com overlay ou botão X funciona
- [ ] Filtro de categoria filtra crises na timeline
- [ ] Link "Linha do Tempo" no TopNav
- [ ] Rota `/dashboard/timeline` acessível

## Teste esperado
- Verificação visual no browser: `/dashboard/timeline`
- Testar: clicar em crise → abrir painel → ver 3 seções → fechar → clicar em evento

## Estimativa de complexidade
alta
~200 linhas de código | ~5 arquivos
