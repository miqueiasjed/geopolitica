# Task 8.6 – AdminBiblioteca, rotas admin e link na TopNav

## Objetivo
Criar a página admin de listagem/gerenciamento de conteúdos, adicionar as rotas admin e incluir o link "Biblioteca" na TopNav do dashboard.

## Tipo
frontend-pagina

## Arquivos a criar/modificar
- `src/pages/admin/AdminBiblioteca.tsx` → criar
- `src/router.tsx` → modificar (rotas `/admin/biblioteca` e `/admin/novo-conteudo`)
- `src/components/layout/TopNav.tsx` → modificar (adicionar link "Biblioteca")

## Contexto necessário (arquivos para ler antes)
- `src/components/layout/TopNav.tsx` → ver estrutura atual dos links de navegação
- `src/router.tsx` → ver guard de rota admin existente
- `src/pages/admin/` → ver padrão de página admin existente

## Skills necessárias
- frontend-design-system

## Especificação

### O que fazer:

1. **`AdminBiblioteca.tsx`** (rota: `/admin/biblioteca`):
   - Tabela com todos os conteúdos (publicados e rascunhos): tipo, título, plano mínimo, status (badge), data publicação
   - Badge de status: publicado → verde; rascunho → cinza
   - Botão "Novo conteúdo" no canto superior direito → navega para `/admin/novo-conteudo`
   - Ação "Despublicar" em cada linha — `DELETE /api/admin/conteudos/{id}` com confirmação modal
   - Ação "Editar" em cada linha → navega para `/admin/editar-conteudo/{id}` (rota a ser criada em plano futuro — colocar botão mas desabilitado com tooltip "Em breve")
   - Paginação simples (próxima/anterior)
   - Usa `useQuery` para `GET /api/admin/conteudos`

2. **Rotas em `src/router.tsx`** (dentro do guard de role `admin`):
   - `/admin/biblioteca` → `<AdminBiblioteca />`
   - `/admin/novo-conteudo` → `<AdminNovoConteudo />`

3. **`TopNav.tsx`** — adicionar link "Biblioteca":
   - Inserir link "Biblioteca" na lista de navegação principal
   - Rota: `/dashboard/biblioteca`
   - Posicionado após "Feed" (ou onde fizer sentido na ordem visual existente)
   - Mesmo estilo dos links existentes (active state, hover)

### Regras de negócio aplicáveis:
- Rotas `/admin/*` devem ser protegidas por guard de role `admin`
- "Despublicar" requer confirmação (modal ou `window.confirm`) antes de chamar a API
- Botão "Editar" desabilitado com tooltip "Em breve" — não navegar

### Critérios de aceitação:
- [ ] `/admin/biblioteca` lista todos os conteúdos com status correto
- [ ] Clicar "Despublicar" pede confirmação e chama `DELETE` após confirmar
- [ ] "Novo conteúdo" navega para `/admin/novo-conteudo`
- [ ] Link "Biblioteca" aparece na TopNav e está ativo na rota correspondente
- [ ] Assinante comum não consegue acessar `/admin/biblioteca` (redirect ou 403)

## Teste esperado
- Verificação manual: navegar em `/admin/biblioteca` com conta admin e conta assinante

## Estimativa de complexidade
média
~130 linhas de código | ~3 arquivos
