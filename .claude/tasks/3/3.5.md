# Task 3.5 – Frontend admin: AdminAssinantes e AdminWebhookEventos

## Objetivo
Criar as páginas React do painel admin para listagem de assinantes com filtros e o log de eventos do webhook com payload expandível, incluindo as rotas admin protegidas.

## Tipo
frontend-pagina

## Arquivos a criar/modificar
- `frontend/src/pages/admin/AdminAssinantes.tsx` → criar
- `frontend/src/pages/admin/AdminWebhookEventos.tsx` → criar
- `frontend/src/App.tsx` → modificar (adicionar rotas admin)

## Contexto necessário (arquivos para ler antes)
- `frontend/src/contexts/AuthContext.tsx` → ver como verificar role `admin`
- `frontend/src/components/RotaProtegida.tsx` → ver como proteger rotas por role
- `frontend/src/lib/axios.ts` → ver instância para chamadas de API

## Skills necessárias
- frontend-design-system

## Especificação

### O que fazer:
1. Criar endpoint backend necessário (em `routes/api.php`):
   - `GET /api/admin/assinantes?search=&plano=&status=` → listagem paginada
   - `GET /api/admin/webhook-eventos?type=&processado=` → listagem paginada com payload
   - Middleware: `auth:sanctum` + verificação de role `admin`
   - Criar `AdminAssinanteController` simples (listar/filtrar)
   - Criar `AdminWebhookController` simples (listar/filtrar)
2. Criar `AdminAssinantes.tsx`:
   - Usar `useQuery` para buscar assinantes com paginação
   - Filtros: busca por email, filtro por plano, filtro por status
   - Tabela com colunas: email, nome, plano, status, ativo, assinado_em
   - Design: tema dark, tabela com hover
3. Criar `AdminWebhookEventos.tsx`:
   - Usar `useQuery` para buscar eventos com paginação
   - Filtros: tipo de evento, processado/pendente
   - Tabela com colunas: event_type, email, processado, created_at
   - Linha expandível ao clicar: mostrar `payload` formatado como JSON
4. Adicionar rotas admin em `App.tsx`:
   ```tsx
   <Route element={<RotaProtegida requiredRole="admin" />}>
     <Route path="/admin/assinantes" element={<AdminAssinantes />} />
     <Route path="/admin/webhook-eventos" element={<AdminWebhookEventos />} />
   </Route>
   ```
5. Atualizar `RotaProtegida.tsx` para aceitar prop `requiredRole` opcional

### Regras de negócio aplicáveis:
- Apenas usuários com role `admin` devem acessar as rotas `/admin/*`
- Payload do webhook deve ser exibido como JSON formatado (pretty-print), não como string raw
- Paginação: 25 itens por página

### Critérios de aceitação:
- [ ] `GET /api/admin/assinantes` retorna lista paginada de assinantes
- [ ] Filtro por email funciona em tempo real (debounce 300ms)
- [ ] Filtro por plano e status funcionam corretamente
- [ ] `GET /api/admin/webhook-eventos` retorna log de eventos
- [ ] Clicar em um evento expande o payload JSON formatado
- [ ] Rotas `/admin/*` redirecionam não-admins para `/`
- [ ] Design dark consistente com o restante da aplicação

## Teste esperado
- Verificação manual: logar como admin, acessar `/admin/assinantes`, testar filtros

## Estimativa de complexidade
alta
~200 linhas de código | ~3 arquivos (+ 2 controllers backend)
