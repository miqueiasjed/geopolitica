# Task 34.4 – EleicaoPage (assinante): EleicaoDetailPanel como drawer em mobile

## Objetivo
Tornar o `EleicaoDetailPanel` comportar-se como drawer full-screen ou modal em mobile (< sm), preservando o layout de grid+painel em desktop.

## Tipo
frontend-pagina

## Arquivos a criar/modificar
- `frontend/src/pages/dashboard/EleicaoPage.tsx` → modificar (versão assinante)

## Contexto necessário (arquivos para ler antes)
- `frontend/src/pages/dashboard/EleicaoPage.tsx` → ver como o painel é aberto (estado, props), layout atual (grid + painel lateral)
- `frontend/src/components/eleicoes/EleicaoDetailPanel.tsx` → ver classes de largura (`sm:w-[440px]`), se já é `fixed` ou `relative`

## Skills necessárias
- `frontend-design-system`
- `animacoes-motion`

## Especificação

### O que fazer:
1. Verificar se `EleicaoDetailPanel` já é um painel `fixed` à direita.
2. Em mobile (< sm), o painel deve ocupar `w-full h-full fixed inset-0 z-50` — cobrir toda a tela.
3. Adicionar header com botão "Voltar" (`← Voltar`) visível apenas em mobile (`sm:hidden`).
4. Em desktop (sm+): manter comportamento atual (`sm:w-[440px]`, posição lateral).
5. Na `EleicaoPage`, o grid de cards de eleições deve usar `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`.
6. Em mobile, quando um card é clicado, o painel cobre a tela toda (voltando ao grid via botão voltar).

**Classes sugeridas para o painel em mobile:**
```tsx
<aside className="fixed inset-0 z-50 bg-background overflow-y-auto sm:relative sm:inset-auto sm:w-[440px] sm:z-auto">
  {/* Header mobile */}
  <div className="sm:hidden flex items-center gap-2 p-4 border-b sticky top-0 bg-background">
    <button onClick={onClose}><ChevronLeftIcon /> Voltar</button>
  </div>
  {/* conteúdo */}
</aside>
```

### Regras de negócio aplicáveis:
- O botão voltar em mobile deve fechar o painel e retornar ao grid de eleições.
- Desktop: painel lateral mantido exatamente como estava.

### Critérios de aceitação:
- [ ] Em 375px: clicar em uma eleição abre painel full-screen com botão voltar.
- [ ] Botão voltar fecha o painel e retorna ao grid.
- [ ] Grid de eleições com 1 coluna em 375px.
- [ ] Em sm+: painel lateral preservado.

## Teste esperado
- Em 375px: selecionar eleição → painel full-screen → voltar → grid de eleições.

## Estimativa de complexidade
média
~30 linhas de código | ~1 arquivo
