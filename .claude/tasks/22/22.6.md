# Task 22.6 – Componente React ExportPdfButton

## Objetivo
Criar o componente reutilizável `ExportPdfButton.tsx` que, ao ser clicado, faz POST para `/api/export-pdf`, recebe o PDF como Blob e dispara o download automático no browser, com estado de loading visível.

## Tipo
frontend-componente

## Arquivos a criar/modificar
- `frontend/src/components/ExportPdfButton.tsx` → criar

## Contexto necessário (arquivos para ler antes)
- `frontend/src/hooks/useAuth.ts` → ver como pegar o token de autenticação para as requisições
- `frontend/src/components/AddonGate.tsx` → ver padrão de estilo dos botões do projeto

## Skills necessárias
- frontend-design-system

## Especificação

### O que fazer:
Criar componente `ExportPdfButton` com as seguintes props:
```typescript
interface ExportPdfButtonProps {
  tipo: 'briefing' | 'alerta' | 'pais' | 'chat' | 'report' | 'risk_score'
  id: string
  label?: string
  companySlug?: string
}
```

Lógica do componente:
1. Estado: `loading: boolean`
2. Ao clicar: `setLoading(true)`, fazer `fetch('/api/export-pdf', { method: 'POST', headers: { Authorization, 'Content-Type': 'application/json' }, body: JSON.stringify({ tipo, id, company_slug: companySlug }) })`
3. Verificar `res.ok` — se não, exibir alerta com mensagem de erro e retornar
4. `const blob = await res.blob()`
5. Criar URL temporária com `URL.createObjectURL(blob)`, criar `<a>` dinâmico, definir `href` e `download`, clicar, revogar URL
6. `setLoading(false)` no `finally`

Estilo seguindo o padrão existente (botão sutil, tons escuros):
```tsx
<button
  onClick={handleExport}
  disabled={loading}
  className="flex items-center gap-2 text-[10px] tracking-widest uppercase
    border border-white/10 px-3 py-1.5 text-white/40
    hover:border-white/25 hover:text-white/60 transition-all
    disabled:opacity-30 disabled:cursor-not-allowed"
>
  {/* ícone de download SVG */ }
  {loading ? 'Gerando...' : label}
</button>
```

Ícone SVG de download (14x14, stroke currentColor):
```
<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
<polyline points="7 10 12 15 17 10"/>
<line x1="12" y1="15" x2="12" y2="3"/>
```

### Regras de negócio aplicáveis:
- Usar `useAuth` para pegar o token de Authorization: Bearer antes do fetch.
- Revogar a URL do Blob imediatamente após o clique para liberar memória.
- Não usar `window.open` — usar elemento `<a>` dinâmico para garantir o download correto.

### Critérios de aceitação:
- [ ] Componente aceita props `tipo`, `id`, `label` e `companySlug`
- [ ] Botão exibe "Gerando..." durante o carregamento e fica desabilitado
- [ ] Download iniciado automaticamente após geração do PDF
- [ ] Erro de API exibido como alerta
- [ ] Ícone de download visível no botão
- [ ] Estilo consistente com o design system escuro do projeto

## Teste esperado
- Verificação manual: adicionar o botão temporariamente em uma página e verificar download
- Será integrado nas páginas na Task 22.7

## Estimativa de complexidade
baixa
~55 linhas de código | ~1 arquivo
