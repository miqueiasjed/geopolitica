# Task 22.5 – ExportPdfController e rota POST /api/export-pdf

## Objetivo
Criar o controller e registrar a rota autenticada `POST /api/export-pdf` que recebe tipo, ID e company_slug opcional, valida a requisição, delega ao `PdfTemplateService` e retorna o PDF como download.

## Tipo
backend-endpoint

## Arquivos a criar/modificar
- `backend/app/Http/Controllers/Api/ExportPdfController.php` → criar
- `backend/routes/api.php` → modificar (adicionar rota)

## Contexto necessário (arquivos para ler antes)
- `backend/app/Http/Controllers/Api/ChatController.php` → padrão de controller do projeto
- `backend/app/Services/PdfTemplateService.php` → interface do service (Task 22.4)
- `backend/routes/api.php` → localizar onde adicionar a nova rota no grupo auth:sanctum

## Skills necessárias
- laravel-arquitetura

## Especificação

### O que fazer:

#### `ExportPdfController.php`
```php
public function exportar(Request $request): \Symfony\Component\HttpFoundation\Response
{
    $request->validate([
        'tipo'          => 'required|string|in:briefing,alerta,pais,chat',
        'id'            => 'required|string',
        'company_slug'  => 'nullable|string|max:100',
    ]);

    $pdf = $this->pdfTemplateService->gerar(
        tipo:         $request->input('tipo'),
        id:           $request->input('id'),
        companySlug:  $request->input('company_slug'),
        userId:       $request->user()->id,
    );

    $filename = 'GPI-' . $request->input('tipo') . '-' . substr($request->input('id'), 0, 8) . '.pdf';

    return response($pdf, 200, [
        'Content-Type'        => 'application/pdf',
        'Content-Disposition' => 'attachment; filename="' . $filename . '"',
    ]);
}
```

Capturar `ModelNotFoundException` e retornar 404. Capturar `InvalidArgumentException` e retornar 422.

#### `routes/api.php`
Adicionar dentro do grupo `auth:sanctum` existente:
```php
Route::post('/export-pdf', [ExportPdfController::class, 'exportar']);
```

### Regras de negócio aplicáveis:
- Rota protegida por `auth:sanctum` — apenas assinantes autenticados podem exportar.
- Nome do arquivo no formato `GPI-{tipo}-{id8}.pdf` onde `id8` são os primeiros 8 chars do ID.
- Content-Type `application/pdf` e header `Content-Disposition: attachment` para forçar download.

### Critérios de aceitação:
- [ ] `POST /api/export-pdf` com tipo e ID válidos retorna PDF (200) com headers corretos
- [ ] Request sem autenticação retorna 401
- [ ] Tipo inválido retorna 422
- [ ] ID inexistente retorna 404
- [ ] Filename no formato `GPI-briefing-abc12345.pdf`

## Teste esperado
- Testar via Insomnia/curl: `POST /api/export-pdf {"tipo":"briefing","id":"<uuid>"}` com token Bearer
- Verificar que o arquivo baixado abre corretamente no leitor de PDF

## Estimativa de complexidade
baixa
~40 linhas de código | ~2 arquivos
