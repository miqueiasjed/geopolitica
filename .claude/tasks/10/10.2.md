# Task 10.2 – DetectorSinaisService (integração Claude API)

## Objetivo
Implementar o serviço de detecção de padrões geopolíticos que analisa eventos recentes via Claude API, classifica-os em sinais `military/diplomatic/supply` e persiste os resultados em `sinais_padrao`.

## Tipo
backend-logica

## Arquivos a criar/modificar
- `app/Services/DetectorSinaisService.php` → criar
- `app/Jobs/DetectarSinaisJob.php` → criar

## Contexto necessário (arquivos para ler antes)
- `app/Models/SinalPadrao.php` → ver campos e relacionamento com `Event`
- `app/Models/Event.php` → ver campos disponíveis para análise (`titulo`, `resumo`, `regiao`, `categorias`)
- `app/Services/` → ver padrão de Service e como outros Services usam HTTP client

## Skills necessárias
- laravel-arquitetura
- claude-api

## Especificação

### O que fazer:

1. **`DetectorSinaisService`**:

   - `detectar(): void`
     - Busca eventos das últimas 48h ainda não analisados:
       ```php
       Event::ultimas48h()
           ->whereNotIn('id', SinalPadrao::select('event_id'))
           ->get()
       ```
     - Processa em lotes de 5 eventos via `chunk(5, fn($lote) => $this->analisarLote($lote))`

   - `analisarLote(Collection $eventos): void`
     - Monta prompt para Claude com os eventos do lote (título, resumo, região, categorias)
     - Chama Claude API: model `claude-haiku-4-5-20251001` (mais rápido/barato para classificação)
     - Prompt instrui retornar JSON com array de sinais:
       ```
       Para cada evento, identifique se há padrão geopolítico.
       Retorne JSON: [{"event_id": N, "tipo_padrao": "military|diplomatic|supply", "nome_sinal": "...", "regiao": "...", "peso": 1-10, "confianca": 0-100}]
       Se o evento não tem padrão relevante, omita-o da resposta.
       ```
     - Parse da resposta JSON da API
     - Para cada sinal no resultado: cria registro em `SinalPadrao`
     - Em caso de erro da API: loga e continua (não interrompe o lote)

   - Usar `Anthropic\Laravel\Facades\Anthropic` (ou `Http::withHeaders` se SDK não disponível):
     ```php
     $resposta = Anthropic::messages()->create([
         'model' => 'claude-haiku-4-5-20251001',
         'max_tokens' => 1024,
         'messages' => [['role' => 'user', 'content' => $prompt]],
     ]);
     ```

2. **`DetectarSinaisJob`**:
   - `implements ShouldQueue`
   - `$queue = 'alertas'`, `$tries = 3`, `$timeout = 120`, `$backoff = 60`
   - `handle()`: chama `DetectorSinaisService::detectar()`

### Regras de negócio aplicáveis:
- Anti-duplicata via `whereNotIn('id', SinalPadrao::select('event_id'))` — evento já analisado não é reprocessado
- Lotes de 5 para não exceder limites de contexto do Claude e controlar custo
- `claude-haiku-4-5-20251001` para classificação (custo menor) — `claude-sonnet-4-6` fica para análise de convergência
- Falha em 1 lote não interrompe os demais (try/catch por lote)

### Critérios de aceitação:
- [ ] `DetectorSinaisService::detectar()` cria registros em `sinais_padrao`
- [ ] Eventos já analisados não são reprocessados em segunda execução
- [ ] Sinais com `confianca < 30` podem ser filtrados (opcional, via config)
- [ ] Falha da API em 1 lote não impede processamento dos demais lotes
- [ ] Job despacha sem erros: `DetectarSinaisJob::dispatch()`

## Teste esperado
- Comando: `php artisan tinker` → `app(DetectorSinaisService::class)->detectar()`
- Verificação: `SinalPadrao::count()` deve aumentar após execução com eventos disponíveis

## Estimativa de complexidade
alta
~120 linhas de código | ~2 arquivos
