# Task 2.5 – Frontend: AuthContext, useAuth e RotaProtegida

## Objetivo
Criar o contexto de autenticação React, o hook `useAuth`, o interceptor Axios com Bearer token e o componente `RotaProtegida` que redireciona usuários não autenticados.

## Tipo
frontend-componente

## Arquivos a criar/modificar
- `frontend/src/contexts/AuthContext.tsx` → criar
- `frontend/src/hooks/useAuth.ts` → criar
- `frontend/src/lib/axios.ts` → modificar (adicionar interceptor Bearer)
- `frontend/src/components/RotaProtegida.tsx` → criar
- `frontend/src/types/auth.ts` → criar

## Contexto necessário (arquivos para ler antes)
- `frontend/src/lib/axios.ts` → ver instância Axios criada na task 1.4
- `frontend/src/main.tsx` → ver providers existentes para envolver com AuthProvider

## Skills necessárias
- frontend-design-system

## Especificação

### O que fazer:
1. Criar `src/types/auth.ts`:
   ```ts
   export interface Assinante { id: number; plano: string; ativo: boolean; status: string }
   export interface User { id: number; name: string; email: string; role: string; assinante?: Assinante }
   export interface AuthState { user: User | null; token: string | null; isAuthenticated: boolean }
   ```
2. Criar `src/contexts/AuthContext.tsx`:
   - Estado: `user`, `token`, `isLoading`
   - `login(email, password)`: chama `POST /api/auth/login`, armazena token em `localStorage`, define user
   - `logout()`: chama `POST /api/auth/logout`, remove token do localStorage, limpa estado
   - `checkAuth()`: ao inicializar, verifica token no localStorage e chama `/api/auth/me`
   - Exportar `AuthContext` e `AuthProvider`
3. Criar `src/hooks/useAuth.ts`:
   ```ts
   export function useAuth() {
     const ctx = useContext(AuthContext)
     if (!ctx) throw new Error('useAuth must be used within AuthProvider')
     return ctx
   }
   ```
4. Atualizar `src/lib/axios.ts`:
   - Adicionar interceptor de request que inclui `Authorization: Bearer {token}` se token existir no localStorage
   - Adicionar interceptor de response para redirecionar para `/login` em caso de 401
5. Criar `src/components/RotaProtegida.tsx`:
   - Se `isLoading`: mostrar spinner
   - Se não autenticado: `<Navigate to="/login" replace />`
   - Se autenticado: `<Outlet />`
6. Envolver app com `<AuthProvider>` no `main.tsx`

### Regras de negócio aplicáveis:
- Token armazenado em `localStorage` com chave `auth_token`
- `checkAuth()` deve ser chamado uma única vez na inicialização do app (useEffect no AuthProvider)
- Se o token expirar (401 no interceptor), limpar estado e redirecionar para login

### Critérios de aceitação:
- [ ] `useAuth()` retorna `{ user, token, isAuthenticated, login, logout }` corretamente
- [ ] Após `login()`, o token é salvo no localStorage e as requisições incluem o header Bearer
- [ ] Após `logout()`, o token é removido do localStorage e o usuário é redirecionado para `/login`
- [ ] `RotaProtegida` redireciona para `/login` quando não autenticado
- [ ] `RotaProtegida` renderiza o filho quando autenticado
- [ ] Ao recarregar a página com token válido, o usuário permanece autenticado

## Teste esperado
- Verificação manual: fazer login, recarregar página, confirmar que continua autenticado

## Estimativa de complexidade
média
~120 linhas de código | ~5 arquivos
