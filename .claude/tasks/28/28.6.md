# Task 28.6 – Ajustar TopNav + MonitorEleitoral + MonitorGuerra

## Objetivo
Adicionar indicador "Cancelado" no TopNav quando addon está cancelado, e renomear a prop `addonKey` para `chave` em MonitorEleitoral e MonitorGuerra para alinhar com a nomenclatura do backend.

## Tipo
frontend-componente

## Arquivos a criar/modificar
- `frontend/src/components/TopNav.tsx` → modificar
- `frontend/src/pages/MonitorEleitoral.tsx` → modificar
- `frontend/src/pages/MonitorGuerra.tsx` → modificar

## Contexto necessário (arquivos para ler antes)
- `frontend/src/components/TopNav.tsx` → entender estrutura atual e onde inserir eyebrow
- `frontend/src/pages/MonitorEleitoral.tsx` → localizar uso de `addonKey` prop
- `frontend/src/pages/MonitorGuerra.tsx` → localizar uso de `addonKey` prop
- `frontend/src/hooks/useProdutos.ts` → para TopNav usar status do addon

## Skills necessárias
- `frontend-design-system`

## Especificação

### O que fazer:
**TopNav.tsx:**
- Importar `useProdutos` e verificar se algum produto tem `status_usuario === 'cancelado'`
- Se sim, exibir eyebrow/badge no TopNav com texto "Cancelado" (ou aplicar à navegação específica do addon)
- Design: badge pequeno, cor âmbar/warning, posicionado próximo ao item de navegação do addon

**MonitorEleitoral.tsx:**
- Localizar onde `AddonGate` é utilizado com prop `addonKey="elections"`
- Renomear para `chave="elections"`
- Verificar outros usos da prop (se houver) e renomear todos

**MonitorGuerra.tsx:**
- Mesma operação: renomear `addonKey="war"` para `chave="war"`

### Regras de negócio aplicáveis:
- Exibir eyebrow "Cancelado" apenas quando addon específico do produto está cancelado
- A renomeação de prop é mecânica — garantir que `AddonGate` aceite `chave` (após Task 28.5)
- Sem regressão visual nas páginas de monitor

### Critérios de aceitação:
- [ ] TopNav exibe indicador "Cancelado" quando addon com status cancelado existe
- [ ] Indicador não aparece para status ativo/null
- [ ] `MonitorEleitoral` compila sem erros após renomeação de prop
- [ ] `MonitorGuerra` compila sem erros após renomeação de prop
- [ ] `tsc --noEmit` sem erros

## Teste esperado
- Verificação: `tsc --noEmit` sem erros no projeto
- Verificação visual: com addon `cancelado`, confirmar eyebrow no TopNav
- Verificação visual: navegar para Monitor — páginas carregam sem erros

## Estimativa de complexidade
baixa
~30 linhas modificadas | ~3 arquivos
