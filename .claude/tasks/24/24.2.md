# Task 24.2 – Models: MapaRiscoAtivo e Carteira

## Objetivo
Criar os dois Eloquent Models para o Risk Score de Portfólio, com fillable, casts e relacionamentos.

## Tipo
backend-estrutura

## Arquivos a criar/modificar
- `backend/app/Models/MapaRiscoAtivo.php` → criar
- `backend/app/Models/Carteira.php` → criar

## Contexto necessário (arquivos para ler antes)
- `backend/app/Models/Assinante.php` → padrão de model com JSON cast
- `backend/database/migrations/2026_04_27_400000_create_mapa_risco_ativos_table.php` → campos (Task 24.1)
- `backend/database/migrations/2026_04_27_400001_create_carteiras_table.php` → campos (Task 24.1)

## Skills necessárias
- laravel-arquitetura

## Especificação

### O que fazer:

#### `MapaRiscoAtivo.php`
```php
namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class MapaRiscoAtivo extends Model
{
    protected $table = 'mapa_risco_ativos';

    public $timestamps = false; // apenas updated_at gerenciado manualmente
    public $updatedAt  = 'updated_at';
    public $createdAt  = null;

    protected $fillable = [
        'ticker', 'name', 'asset_type', 'risk_weights', 'regions', 'notes',
    ];

    protected function casts(): array
    {
        return [
            'risk_weights' => 'array',
            'regions'      => 'array',
        ];
    }
}
```

#### `Carteira.php`
```php
namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class Carteira extends Model
{
    protected $table = 'carteiras';

    protected $fillable = [
        'user_id', 'nome', 'ativos', 'ultimo_score',
    ];

    protected function casts(): array
    {
        return [
            'ativos'       => 'array',
            'ultimo_score' => 'array',
        ];
    }

    public function user(): BelongsTo
    {
        return $this->belongsTo(User::class);
    }
}
```

### Regras de negócio aplicáveis:
- `MapaRiscoAtivo` não tem `created_at` — timestamps manual apenas em `updated_at`.
- `risk_weights` e `regions` são castados para `array` para facilitar manipulação PHP.
- `Carteira.ativos` e `ultimo_score` são `array` — estruturas JSON que o RiskScoreService manipula.

### Critérios de aceitação:
- [ ] `MapaRiscoAtivo` com `$timestamps = false`, fillable completo e casts de array
- [ ] `Carteira` com casts de `ativos` e `ultimo_score` e relacionamento com User
- [ ] `MapaRiscoAtivo::where('ticker', 'PETR4')->first()->risk_weights` retorna array PHP
- [ ] `new Carteira()` instancia sem erros

## Teste esperado
- Via `php artisan tinker`: `App\Models\MapaRiscoAtivo::create(['ticker' => 'TEST', 'name' => 'Test', 'risk_weights' => ['energy' => 0.5, 'food' => 0, 'fx' => 0, 'military' => 0]])`

## Estimativa de complexidade
baixa
~50 linhas de código | ~2 arquivos
