# Task 7.5 – AdminConteudoController (CRUD admin)

## Objetivo
Implementar o CRUD completo de conteúdos para o role `admin`: listar rascunhos e publicados, criar, atualizar/publicar e despublicar. Adicionar as rotas admin correspondentes.

## Tipo
backend-endpoint

## Arquivos a criar/modificar
- `app/Http/Controllers/Api/Admin/AdminConteudoController.php` → criar
- `routes/api.php` → modificar (adicionar rotas admin de conteúdos)

## Contexto necessário (arquivos para ler antes)
- `app/Services/ConteudoService.php` → entender assinatura de `criar()`, `atualizar()`, `despublicar()`
- `app/Http/Controllers/Api/Admin/` → ver padrão de controller admin existente (se houver)
- `routes/api.php` → ver grupo de rotas admin existente

## Skills necessárias
- laravel-arquitetura
- permissoes-e-sync

## Especificação

### O que fazer:

1. **`AdminConteudoController`**:

   - `index(Request $request): JsonResponse`
     - Lista todos os conteúdos (publicados e rascunhos), sem filtro de plano
     - Paginação simples (`paginate(20)`)
     - Retorna coleção de `ConteudoResource`

   - `store(CriarConteudoRequest $request): JsonResponse`
     - Chama `ConteudoService::criar($request->validated())`
     - Retorna `response()->json(new ConteudoResource($conteudo), 201)`

   - `update(AtualizarConteudoRequest $request, Conteudo $conteudo): JsonResponse`
     - Chama `ConteudoService::atualizar($conteudo, $request->validated())`
     - Retorna `response()->json(new ConteudoResource($conteudo))`

   - `destroy(Conteudo $conteudo): JsonResponse`
     - Chama `ConteudoService::despublicar($conteudo)`
     - Retorna `response()->json(['message' => 'Conteúdo despublicado com sucesso'])`

   - Middleware no construtor: `auth:sanctum` + `role:admin` (Spatie)

2. **Rotas em `routes/api.php`** (grupo admin com `auth:sanctum` + `role:admin`):
   ```php
   Route::middleware(['auth:sanctum', 'role:admin'])->prefix('admin')->group(function () {
       Route::get('/conteudos', [AdminConteudoController::class, 'index']);
       Route::post('/conteudos', [AdminConteudoController::class, 'store']);
       Route::patch('/conteudos/{conteudo}', [AdminConteudoController::class, 'update']);
       Route::delete('/conteudos/{conteudo}', [AdminConteudoController::class, 'destroy']);
   });
   ```

### Regras de negócio aplicáveis:
- `DELETE /api/admin/conteudos/{id}` é despublicação lógica, nunca exclusão física
- Apenas role `admin` pode acessar essas rotas (403 para outros roles)
- `PATCH` aceita campos parciais (via `AtualizarConteudoRequest` com campos nullable)
- Admin vê todos os conteúdos, incluindo rascunhos (`publicado = false`)

### Critérios de aceitação:
- [ ] `POST /api/admin/conteudos` com token admin cria conteúdo e retorna HTTP 201
- [ ] `PATCH /api/admin/conteudos/{id}` com `publicado: true` define `publicado_em` automaticamente
- [ ] `DELETE /api/admin/conteudos/{id}` retorna HTTP 200 e o conteúdo permanece no banco com `publicado = false`
- [ ] `GET /api/admin/conteudos` com token de `assinante_pro` retorna HTTP 403
- [ ] `GET /api/admin/conteudos` sem token retorna HTTP 401
- [ ] `POST /api/admin/conteudos` com dados inválidos retorna HTTP 422

## Teste esperado
- Comando: `php artisan test --filter=AdminConteudoControllerTest`
- Verificação manual: `php artisan route:list | grep admin/conteudos`

## Estimativa de complexidade
média
~90 linhas de código | ~2 arquivos
